resources:
  - name: christianb
    type: GitRepo
    configuration:
      gitProvider: chb0github
      path: chb0github/jfrog-pipelines-health-check
pipelines:
  - name: christianb_pipeline_health_check
    steps:
      - name: health_check
        type: Bash
        configuration:
          priority: 0
          environmentVariables:
            ROUTING_KEY: '${int_PagerDutyRoutingKey_NOTIFY_ROUTING_KEY}'
          integrations:
            - name: PagerDuty
            - name: PartnerEngineeringPagerDuty
          inputResources:
            - name: christianb
        execution:
          onExecute:
            - echo "Running $pipeline_name | $step_name on node ID '$ROUTING_KEY'"
